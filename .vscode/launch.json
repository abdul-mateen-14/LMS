{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Library GUI",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/LibraryGUI.exe",
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "gdb.exe"
        }
    ]
}
